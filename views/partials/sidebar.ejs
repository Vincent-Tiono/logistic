<nav class="sidebar">
  <div class="menu-group-title">Main</div>
  <a class="nav-item-link" href="/home">
    <span>🏠</span><span>Home</span>
  </a>

  <% if ((divisi || '').toUpperCase() === 'IT') { %>
    <div class="menu-group-title">IT</div>
    <a class="nav-item-link" href="/create-user">
      <span>👤</span><span>Create User</span>
    </a>
  <% } %>

  <% if ((divisi || '').toUpperCase() === 'IT' || (divisi || '').toUpperCase() === 'OPERATION') { %>
    <div class="menu-group-title">Operation</div>
    <a class="nav-item-link" href="/vessel">
      <span>🚢</span><span>Vessel</span>
    </a>
    <a class="nav-item-link" href="/shipper">
      <span>📦</span><span>Shipper</span>
    </a>
  <% } %>

  <div class="menu-group-title text-muted small px-3">
    Other modules still on the PHP app during migration.
  </div>
</nav>
